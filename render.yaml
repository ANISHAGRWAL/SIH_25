services:
  - type: web
    name: speech-backend
    runtime: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r backend/speech/requirements.txt
    startCommand: uvicorn backend.speech.app:app --host 0.0.0.0 --port $PORT
