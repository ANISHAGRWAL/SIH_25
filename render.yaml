services:
  - type: web
    name: speech-backend
    runtime: python
    plan: free
    branch: main
    rootDirectory: backend/speech
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
