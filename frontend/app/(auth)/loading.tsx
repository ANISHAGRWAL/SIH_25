import PageLoader from "@/components/ui/page-loader";

export default function Loading() {
  return (
    <PageLoader
      title="Securing your session"
      subtitle="Preparing authentication experience..."
      compact
    />
  );
}
